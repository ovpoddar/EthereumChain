using Microsoft.Extensions.Hosting;

namespace Miner.Processors;
internal static class ChainSetUpProcesser
{
    public static void RunInitilized(this IHost host)
    {
        // check if chain is setup or not
        // Pull the block chain from API some how or possibly from other nodes
        //
    }
}
